__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
dist/
build/
wheelhouse/
*.egg-info/
*.db
*.db-shm
*.db-wal
.coverage
coverage/
htmlcov/
*.lcov
.wheel-smoke/
.claude/settings.local.json
.claude/.cache/
.claude/state/

# Multi-agent lock patterns (preserving explicit build requirements if present)
LOCK
LOCK.*
LOCK*.txt
*.lock
LOCK.permissions.json
uv.lock
!package-lock.json

# Synchronization conflict patterns
*-conflict-*
*.sync-conflict-*
*.conflict
*-CONFLIT-*
*.sync-temp-*
* (kopie)*
* (Kopie)*
* (copy)*
* (Copy)*
*conflicted copy*

# Multi-host tokens
*-WORKSTATION*
*-WORKSTATION-LG*
*-ASUS*
*-ASUS-GEI*
*-LAPTOP*

# Environment and packaging caches
.tox/
.turbo/

# Temporary, patch, editor and log artifacts
*.tmp
*.bak
*.orig
*.rej
*.swp
*~
*.log

# Internal Plan-D working pointer (local development bookkeeping, not published)
PLAN_D_POINTER.md

# Internal business reports (local, not published)
_reports/internal-business/
